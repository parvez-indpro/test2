/*
 * TheHeader Messages
 *
 * This contains all the text for the TheHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TheHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TheHeader component!',
  },
});
