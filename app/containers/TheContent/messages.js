/*
 * TheContent Messages
 *
 * This contains all the text for the TheContent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TheContent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TheContent container!',
  },
});
