/*
 * BasicForms Messages
 *
 * This contains all the text for the BasicForms container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BasicForms';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BasicForms container!',
  },
});
