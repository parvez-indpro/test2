/*
 * TheLayout Messages
 *
 * This contains all the text for the TheLayout container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TheLayout';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TheLayout container!',
  },
});
