/*
 * TablesSample Messages
 *
 * This contains all the text for the TablesSample container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TablesSample';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TablesSample container!',
  },
});
