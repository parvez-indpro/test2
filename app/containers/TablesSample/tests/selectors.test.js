// import { selectTablesSampleDomain } from '../selectors';

describe('selectTablesSampleDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
